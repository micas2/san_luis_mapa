name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - uses: wilhelmsendk/wake-up-supabase@v1
        with:
          projects: ${{ secrets.SUPABASE_PROJECTS_JSON }}